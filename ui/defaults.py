# -*- coding: utf-8 -*-
from __future__ import annotations

# Centralised defaults to avoid NameError when UI/mixins are modularised.
# Keep these minimal and stable.

# Main generator script (people/player generator)
DEFAULT_GENERATE_SCRIPT = "fm26_people_generator.py"

# XML appender script (used by XML Appender tab)
DEFAULT_XML_APPENDER_SCRIPT = "fm26_xml_appender.py"
